<%= simple_form_for(entry) do |f| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :book_id, input_html: {value: params[:book_id]}, as: :hidden %>
  </div>

  <div class="field">
    <%= f.input :c_created_at %>
  </div>

  <div class="field">
    <%= f.input :title %>
  </div>

  <div class="field">
    <%= f.input :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
